{
    "name": "{{ manifest.name|trans }}",
    "short_name": "{{ manifest.short_name|trans }}",
    "description": "{{ manifest.description|trans }}",
    "icons": [
        {
            "src": "{{ manifest.icon }}",
            "sizes": "192x192",
            "type": "image/png"
        }
    ],
    "theme_color": "#ffffff",
    "background_color": "#ffffff",
    "start_url": "{{ manifest.url }}",
    "display": "standalone"
}
